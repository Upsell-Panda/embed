{
  "dependencies": {
    "esbuild": "^0.8.5"
  },
  "scripts": {
    "build": "rm build/* && esbuild src/stylesheet.css --minify --outfile=build/stylesheet.css && cp src/upsell_panda.js build/upsell_panda.js && esbuild build/upsell_panda.js --minify --bundle --outfile=dist/upsell_panda.min.js --loader:.css=text",
    "purge-cdn": "curl https://purge.jsdelivr.net/gh/Upsell-Panda/embed@master/dist/upsell_panda.min.js"
  }
}
